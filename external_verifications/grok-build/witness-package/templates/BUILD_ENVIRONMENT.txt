# BUILD_ENVIRONMENT.txt — required fields (evidence_schema_version=1, 1.0.0-rc4)
#
# Container-owned. Field names mirror the container's
# `BEGIN_SCHEMA_BLOCK BUILD_ENVIRONMENT` writer (record_build_environment /
# init placeholder) exactly. cargo_incremental must be 0, rust_image must
# reference the pinned image digest, grok_build_commit must equal the pinned
# commit, expected_cargo_lock_sha256 the pinned Cargo.lock SHA-256, and
# canonical_build_command the exact build command.
#
# On a pre-container failure path this file may instead be the host's
# `status=NOT_REACHED` placeholder (accepted by the validator for non-started /
# infrastructure outcomes).
evidence_schema_version=1
status=OK
outcome=RECORDED
docker_platform=linux/amd64
network_mode=bridge
rust_image=docker.io/library/rust@sha256:6ca5ad23231207874325a751b9df584d51cd42c066c74c6963c264e3233c3e8e
workdir=/src
home=/work/home
cargo_home=/work/cargo-home
cargo_target_dir=/work/cargo-target
bootstrap_cargo_target_dir=/work/bootstrap-cargo-target
cargo_incremental=0
dotslash_cache=/work/dotslash-cache
path=/work/cargo-home/bin:/usr/local/cargo/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
grok_build_commit=98c3b2438aa922fbbe6178a5c0a4c48f85edc8ce
expected_cargo_lock_sha256=1512bb4fef0c1166c6a15a3398da9593903be1759b759ce78d9958913e61b421
canonical_build_command=cargo build -p xai-grok-pager-bin --locked
mount_src=/src:ro
mount_work=/work:rw
mount_evidence=/evidence:rw
mount_container_script=/witness/container_narrow_build.sh:ro
