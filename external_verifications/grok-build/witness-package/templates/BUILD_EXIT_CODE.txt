# BUILD_EXIT_CODE.txt — required fields (evidence_schema_version=1, 1.0.0-rc4)
#
# Container-owned and AUTHORITATIVE for `outcome`. Field names mirror the
# container's `BEGIN_SCHEMA_BLOCK BUILD_EXIT_CODE` writer exactly.
#
# outcome is one of: BUILD_NOT_STARTED | CARGO_FAILED |
#   CARGO_SUCCEEDED_ARTIFACT_MISSING | CARGO_SUCCEEDED_ARTIFACT_PRESENT |
#   INFRASTRUCTURE_FAILURE
# cargo_exit_code is "0" on success, a nonzero integer on CARGO_FAILED, and
# exactly NOT_APPLICABLE (never "N/A") when cargo never started.
# status is OK when the run fully succeeded (artifact present AND static
# inspection complete) and FAILED otherwise.
evidence_schema_version=1
status=OK
outcome=CARGO_SUCCEEDED_ARTIFACT_PRESENT
cargo_started=YES
build_status=COMPLETE
cargo_exit_code=0
failure_stage=NOT_APPLICABLE
