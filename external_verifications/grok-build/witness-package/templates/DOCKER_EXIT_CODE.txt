# DOCKER_EXIT_CODE.txt — required fields (evidence_schema_version=1, 1.0.0-rc4)
#
# Host-owned record of the `docker run` invocation. docker_exit_code is numeric
# (or NOT_STARTED / NOT_REACHED when the container never launched, e.g. an image
# pull / inspect failure). product_executed and ldd_used must be exactly NO.
# outcome must equal the authoritative BUILD_EXIT_CODE.txt outcome.
evidence_schema_version=1
status=RECORDED
docker_started_utc=<UTC timestamp>
docker_finished_utc=<UTC timestamp>
docker_exit_code=0
container_platform=linux/amd64
network_mode=bridge
product_executed=NO
ldd_used=NO
outcome=CARGO_SUCCEEDED_ARTIFACT_PRESENT
failure_stage=NONE
