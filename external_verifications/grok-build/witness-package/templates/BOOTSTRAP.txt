# BOOTSTRAP.txt — required fields (evidence_schema_version=1, 1.0.0-rc4)
#
# In-container bootstrap of the pinned toolchain support (apt packages,
# dotslash, protoc descriptor). dotslash_version must equal 0.5.7 and
# product_executed must be exactly NO. protoc_descriptor_*_sha256 are 64-char
# lowercase hex; protoc_version_exit_code is numeric.
#
# On a pre-bootstrap / pre-container failure path this file may instead be the
# host's `status=NOT_REACHED` placeholder (the container never reached
# bootstrap); the validator accepts that placeholder for non-started /
# infrastructure outcomes.
evidence_schema_version=1
apt_packages=ca-certificates git build-essential pkg-config cmake curl perl file binutils
dotslash_version=0.5.7
dotslash_binary_path=/work/cargo-home/bin/dotslash
protoc_descriptor_src=/src/bin/protoc
protoc_descriptor_writable=yes
protoc_descriptor_src_sha256=<64-char lowercase hex sha256>
protoc_descriptor_lf_sha256=<64-char lowercase hex sha256>
PROTOC=/work/bootstrap/protoc-descriptor.lf
protoc_version_output=libprotoc <version>
protoc_version_exit_code=0
product_executed=NO
