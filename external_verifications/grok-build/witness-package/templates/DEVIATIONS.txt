# DEVIATIONS.txt — final-submission Witness-input template (evidence_schema_version=1, RC6-R5)
#
# Owner: Witness (final-submission). Host automated preliminary DEVIATIONS use a
# distinct host-owned schema (evidence_schema_version, deviation_state,
# deviation_count, automated_summary) and must not fabricate Witness indexed
# statements.
#
# Final packages must bind to the Host preliminary bytes via
# preliminary_deviations_sha256 (SHA-256 of the preliminary DEVIATIONS.txt).
# run_id must equal the package run_id. Aggregate severity, aggregate
# canonicality impact, and final_machine_ceiling must match validator
# recomputation. When PRESENT, indices are numeric and contiguous 1..n.
#
# Templates remain subordinate to schemas/canonical_schema_register_rc6.json
# (active rc6.4).
evidence_schema_version=1
run_id=<run_id>
deviation_state=NONE
deviation_count=0
preliminary_deviations_sha256=<sha256_of_host_preliminary_deviations_bytes>
aggregate_severity=NONE
aggregate_canonical_identity_impact=no
final_machine_ceiling=PASS
